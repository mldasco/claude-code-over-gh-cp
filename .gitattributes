# Ensure Unix shell scripts always use LF (prevents breakage on macOS/Linux)
run        text eol=lf
*.sh       text eol=lf

# Ensure Windows entry points use CRLF
run.cmd    text eol=crlf
run.ps1    text eol=crlf

# Python scripts - auto (LF on checkout for all platforms is fine)
*.py       text
